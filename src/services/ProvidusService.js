const axios = require('axios');
const Axios = axios.create();

class ProvidusService {
  static async fundWithProvidus(reference) {
    // return new Promise((resolve, reject) => {
    //   const {data} = Axios.post(`postingrest/GetNIPTransactionStatus`, {      reference: reference);
    // });
  }
}
